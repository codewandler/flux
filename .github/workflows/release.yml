# This file was autogenerated by dist: https://axodotdev.github.io/cargo-dist
#
# Copyright 2022-2024, axodotdev
# SPDX-License-Identifier: MIT or Apache-2.0
#
# CI that:
#
# * prepares artifacts for an exact release commit when manually dispatched
# * promotes those immutable artifacts when the matching Git tag arrives
# * falls back to building on the tag when no prepared candidate exists
# * uploads the artifacts to a GitHub Release and verifies its public asset shape
#
# Note that the GitHub Release will be created with a generated
# title/body based on your changelogs.

name: Release
permissions:
  contents: read
  actions: read

# release.yml is dist-generated. Re-apply this ownership override after `dist generate`; the
# structural release/inventory checks reject regeneration that restores an unowned `dist build`.
env:
  CARGO_TARGET_DIR: target

# This task will run whenever you push a git tag that looks like a version
# like "1.0.0", "v0.1.0-prerelease.1", "my-app/0.1.0", "releases/v1.0.0", etc.
# Various formats will be parsed into a VERSION and an optional PACKAGE_NAME, where
# PACKAGE_NAME must be the name of a Cargo package in your workspace, and VERSION
# must be a Cargo-style SemVer Version (must have at least major.minor.patch).
#
# If PACKAGE_NAME is specified, then the announcement will be for that
# package (erroring out if it doesn't have the given version or isn't dist-able).
#
# If PACKAGE_NAME isn't specified, then the announcement will be for all
# (dist-able) packages in the workspace with that version (this mode is
# intended for workspaces with only one dist-able package, or with all dist-able
# packages versioned/released in lockstep).
#
# If you push multiple tags at once, separate instances of this workflow will
# spin up, creating an independent announcement for each one. However, GitHub
# will hard limit this to 3 tags per commit, as it will assume more tags is a
# mistake.
#
# If there's a prerelease-style suffix to the version, then the release(s)
# will be marked as a prerelease.
on:
  pull_request:
  workflow_dispatch:
    inputs:
      version:
        description: "Plain X.Y.Z workspace version to prepare from release-candidates/vX.Y.Z"
        required: true
        type: string
  push:
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'

# A tag pushed while its exact candidate is still building waits for that build, then promotes it.
# Different release commits remain independent.
concurrency:
  group: release-${{ github.sha }}
  cancel-in-progress: false

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ github.event_name == 'workflow_dispatch' && format('v{0}', inputs.version) || (startsWith(github.ref, 'refs/tags/') && github.ref_name || '') }}
      tag-flag: ${{ github.event_name == 'workflow_dispatch' && format('--tag=v{0}', inputs.version) || (startsWith(github.ref, 'refs/tags/') && format('--tag={0}', github.ref_name) || '') }}
      publishing: ${{ startsWith(github.ref, 'refs/tags/') }}
      preparing: ${{ github.event_name == 'workflow_dispatch' }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
          submodules: recursive
      - name: Validate release-candidate request
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          CANDIDATE_VERSION: ${{ inputs.version }}
          DISPATCH_REF: ${{ github.ref }}
        run: |
          expected_ref="refs/heads/release-candidates/v$CANDIDATE_VERSION"
          if [ "$DISPATCH_REF" != "$expected_ref" ]; then
            echo "::error::release candidate v$CANDIDATE_VERSION must be prepared from $expected_ref, got $DISPATCH_REF"
            exit 1
          fi
          workspace_version=$(grep -m1 '^version = ' Cargo.toml | sed -E 's/.*"([^"]+)".*/\1/')
          if [ "$CANDIDATE_VERSION" != "$workspace_version" ]; then
            echo "::error::candidate version $CANDIDATE_VERSION does not match workspace version $workspace_version"
            exit 1
          fi
      # The candidate ref already points at cut-release.sh's committed transaction. Rebuild the
      # public website from its lockfile and prove that exact commit owns the matching server
      # archive before spending release-build time or permitting a candidate receipt.
      - name: Install pinned website toolchain for the exact candidate
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: website/package-lock.json
      - name: Install pinned website dependencies
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: npm ci
        working-directory: website
      - name: Embedded documentation matches the exact candidate SHA
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: scripts/build-embedded-docs.sh --check
      - name: Install pinned release-gate toolchain
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master
        with:
          toolchain: "1.97.0"
          components: rustfmt, clippy
      - name: Cache release-gate cargo state
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Mandatory full gate for the exact candidate SHA
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: scripts/release-full-gate.sh "$GITHUB_SHA"
      # C-354: the plan job does not read RELEASE_TOKEN, not even to test that it is configured.
      # `secrets.RELEASE_TOKEN != ''` is still a reference to the credential from a job that plans,
      # builds and holds the model-visible manifest — and its absence is reported by the one job
      # that actually holds it, in the `release` environment, before anything is published.
      - name: Install dist
        shell: bash
        run: scripts/install-release-tooling.sh
      # sure would be cool if github gave us proper conditionals...
      # so here's a doubly-nested ternary-via-truthiness to try to provide the best possible
      # functionality based on whether this is a pull_request, and whether it's from a fork.
      # (PRs run on the *source* but secrets are usually on the *target* -- that's *good*
      # but also really annoying to build CI around when it needs secrets to work right.)
      # C-412: this job PLANS, and holds no hosting verb.
      #
      # ⚠ Read this before assuming the old `dist host --steps=create` here was what published
      # v0.47.0 — it was not, and that mistake is easy to make twice. In the pinned dist 0.32.0 the
      # `HostingStyle::Github` arm for `create` is empty, so the call was inert: locally,
      # `dist host --steps=create --tag=X` and `dist plan --tag=X` emit byte-identical manifests
      # apart from the build-id string, and running the former against the live repo left v0.47.1's
      # Release untouched. The Release is created by `gh release create` in the `host` job, and the
      # real defect is that it published before the artifact set was checked — closed there, not
      # here.
      #
      # `dist plan` is used anyway, for two smaller reasons: a planning job should not hold a verb
      # that asks to create a public object, and `create` is inert only by an upstream implementation
      # detail that a future dist version may fill in.
      - id: plan
        env:
          CANDIDATE_VERSION: ${{ inputs.version }}
        run: |
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            dist plan --tag="v$CANDIDATE_VERSION" --allow-dirty --output-format=json > plan-dist-manifest.json
          elif [[ "$GITHUB_REF" = refs/tags/* ]]; then
            dist plan --tag="$GITHUB_REF_NAME" --allow-dirty --output-format=json > plan-dist-manifest.json
          else
            dist plan --allow-dirty --output-format=json > plan-dist-manifest.json
          fi
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          # Only the CI fan-out shape crosses the job boundary — never the announcement prose.
          #
          # C-412, the other half: the full manifest embeds the release body, i.e. the CHANGELOG
          # entry being shipped. On the 0.47.0 candidate run Actions matched a masked secret inside
          # that prose and dropped the whole output — `Skip output 'val' since it may contain
          # secret`, a WARNING, not an error. `fromJson(needs.plan.outputs.val)` then had nothing,
          # every build job's `if` went false, all five platform builds plus the global build
          # silently skipped, and the run still concluded `success`. Nothing downstream reads this
          # output except the build matrix and `pr_run_mode`, so nothing downstream needs the prose,
          # and a value with no prose in it cannot collide with a secret mask.
          jq -c '{ci: {github: {artifacts_matrix: .ci.github.artifacts_matrix, pr_run_mode: .ci.github.pr_run_mode}}}' \
            plan-dist-manifest.json > plan-ci-manifest.json
          echo "manifest=$(cat plan-ci-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json
          retention-days: 14

  # A tag may reuse only a successful manual run of this workflow at the tag's exact commit. The
  # receipt repeats the version, SHA, and immutable run ID so lookup cannot degrade to version-only.
  resolve-release-candidate:
    needs: plan
    runs-on: ubuntu-22.04
    outputs:
      run-id: ${{ steps.select.outputs.run-id }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - id: lookup
        name: Find successful exact-SHA candidate
        if: ${{ needs.plan.outputs.publishing == 'true' }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          if ! [[ "$GITHUB_REF_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::warning::$GITHUB_REF_NAME is not a plain vX.Y.Z tag; using the compatibility build path"
            exit 0
          fi
          run_id=$(scripts/find-release-candidate.sh "$GITHUB_REPOSITORY" "$GITHUB_SHA")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "run-id=$run_id" >> "$GITHUB_OUTPUT"
      - name: Download candidate receipt
        if: ${{ steps.lookup.outputs.run-id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-candidate-receipt
          path: candidate
          run-id: ${{ steps.lookup.outputs.run-id }}
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
      - id: select
        name: Verify candidate provenance or select fallback
        env:
          CANDIDATE_RUN_ID: ${{ steps.lookup.outputs.run-id }}
          VERSION: ${{ steps.lookup.outputs.version }}
        run: |
          if [ -z "$CANDIDATE_RUN_ID" ]; then
            if [ "${{ needs.plan.outputs.publishing }}" = "true" ]; then
              echo "::warning::no prepared candidate exists for $GITHUB_SHA; rebuilding all release targets"
              echo "### Release candidate: compatibility rebuild" >> "$GITHUB_STEP_SUMMARY"
              echo "No successful prepared candidate was found at \`$GITHUB_SHA\`." >> "$GITHUB_STEP_SUMMARY"
            fi
            exit 0
          fi
          scripts/release-candidate.sh verify candidate/release-candidate.txt \
            "$VERSION" "$GITHUB_SHA" "$CANDIDATE_RUN_ID"
          echo "run-id=$CANDIDATE_RUN_ID" >> "$GITHUB_OUTPUT"
          echo "### Release candidate: promoted without rebuilding" >> "$GITHUB_STEP_SUMMARY"
          echo "Candidate run: [$CANDIDATE_RUN_ID]($GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$CANDIDATE_RUN_ID)" >> "$GITHUB_STEP_SUMMARY"

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
      - resolve-release-candidate
    if: ${{ always() && needs.plan.result == 'success' && needs.resolve-release-candidate.result == 'success' && fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null && (needs.plan.outputs.preparing == 'true' || (needs.plan.outputs.publishing == 'true' && needs.resolve-release-candidate.outputs.run-id == '') || fromJson(needs.plan.outputs.val).ci.github.pr_run_mode == 'upload') }}
    strategy:
      fail-fast: false
      # Target platforms/runners are computed by dist in create-release.
      # Each member of the matrix has the following arguments:
      #
      # - runner: the github runner
      # - dist-args: cli flags to pass to dist
      # - install-dist: expression to run to install dist on the runner
      #
      # Typically there will be:
      # - 1 "global" task that builds universal installers
      # - N "local" tasks that build each platform's binaries and platform-specific installers
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
          submodules: recursive
      - name: Require preinstalled Rust
        shell: bash
        run: |
          if ! command -v cargo > /dev/null 2>&1; then
            echo "::error::release runners must provide a preinstalled, image-authenticated Rust toolchain"
            exit 1
          fi
      - name: Install dist
        shell: bash
        run: scripts/install-release-tooling.sh
      # Get the dist-manifest
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        run: |
          ${{ matrix.packages_install }}
      - name: Build artifacts (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          # Actually do builds and make zips and whatnot
          scripts/run-python3.sh scripts/build_ownership.py shared --workspace-root "$PWD" -- dist build ${{ needs.plan.outputs.tag-flag }} --allow-dirty --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo "dist ran successfully"
      - name: Build artifacts (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          rem release.yml is generated; keep cargo-dist under the same pre-Cargo target lease.
          scripts\run-python3.cmd scripts\build_ownership.py shared --workspace-root "%CD%" -- dist build ${{ needs.plan.outputs.tag-flag }} --allow-dirty --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo dist ran successfully
      - id: cargo-dist
        name: Post-build
        # We force bash here just because github makes it really hard to get values up
        # to "real" actions without writing to env-vars, and writing to env-vars has
        # inconsistent syntax between shell and powershell.
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          dist print-upload-files-from-manifest --manifest dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
          retention-days: 14

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    needs:
      - plan
      - build-local-artifacts
    runs-on: "ubuntu-22.04"
    env:
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install dist
        shell: bash
        run: scripts/install-release-tooling.sh
      # Get all the local artifacts for the global tasks to use (for e.g. checksums)
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        run: |
          # release.yml is generated; keep cargo-dist under the same pre-Cargo target lease.
          scripts/run-python3.sh scripts/build_ownership.py shared --workspace-root "$PWD" -- dist build ${{ needs.plan.outputs.tag-flag }} --allow-dirty --output-format=json "--artifacts=global" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          jq --raw-output ".upload_files[]" dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
          retention-days: 14

  # A preparation run becomes eligible for promotion only after every cargo-dist build succeeds.
  # Its receipt is deliberately separate from `artifacts-*`, so it can never become a release asset.
  #
  # C-412: this job runs whenever the workflow is PREPARING, and fails when the builds it certifies
  # did not run. It used to gate itself on those builds succeeding, which meant a run whose builds
  # all skipped skipped this job too — and a workflow with no failing job concludes `success`. That
  # is the signal a human reads before pushing the tag, and for v0.47.0 it said the candidate was
  # ready when nothing had been compiled at all. A skipped build is now a red run.
  record-release-candidate:
    needs:
      - plan
      - build-local-artifacts
      - build-global-artifacts
    if: ${{ always() && needs.plan.outputs.preparing == 'true' && needs.plan.result == 'success' }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - name: Every release target was actually built
        env:
          PLAN_MANIFEST: ${{ needs.plan.outputs.val }}
          LOCAL_RESULT: ${{ needs.build-local-artifacts.result }}
          GLOBAL_RESULT: ${{ needs.build-global-artifacts.result }}
        run: |
          if [ -z "$PLAN_MANIFEST" ]; then
            echo "::error::the plan job produced no build matrix for this run, so no build job could" \
              "be scheduled. If the job log shows \"Skip output 'val' since it may contain secret\"," \
              "the manifest matched a masked repository secret and Actions discarded it."
            exit 1
          fi
          failed=0
          for job in "build-local-artifacts:$LOCAL_RESULT" "build-global-artifacts:$GLOBAL_RESULT"; do
            if [ "${job#*:}" != "success" ]; then
              echo "::error::${job%%:*} did not run to success (${job#*:}); this candidate built nothing to promote"
              failed=1
            fi
          done
          [ "$failed" -eq 0 ]
      # C-355: the receipt binds the seven uploads this run actually produced — name, immutable
      # database ID, size and GitHub's own SHA-256 — read from the artifacts API (paginated) after
      # every producer job has succeeded and its upload is final. Identity never comes from a file
      # inside an archive: a checksum written by the run authenticates neither the transport nor the
      # API handoff.
      - name: Write exact-SHA candidate receipt
        env:
          VERSION: ${{ inputs.version }}
          GH_TOKEN: ${{ github.token }}
        run: |
          scripts/release-candidate.sh write release-candidate.txt \
            "$VERSION" "$GITHUB_SHA" "$GITHUB_RUN_ID"
          cat release-candidate.txt
      - name: Upload candidate receipt
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-candidate-receipt
          path: release-candidate.txt
          if-no-files-found: error
          retention-days: 14
      - name: Summarize prepared candidate
        env:
          VERSION: ${{ inputs.version }}
        run: |
          echo "### Release candidate prepared" >> "$GITHUB_STEP_SUMMARY"
          echo "- Version: \`$VERSION\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: \`$GITHUB_SHA\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Candidate run: \`$GITHUB_RUN_ID\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Retention: 14 days; after expiry the tag workflow rebuilds explicitly" >> "$GITHUB_STEP_SUMMARY"

  # Assembles and CHECKS the exact asset set. C-354: this job holds no publication authority at all
  # — no environment, no write permission, no long-lived secret. Everything it produces is handed to
  # the two authority-bearing jobs below as an ordinary workflow artifact, so a defect here cannot
  # reach a signing key, a Release credential or the attestation identity.
  host:
    needs:
      - plan
      - resolve-release-candidate
      - build-local-artifacts
      - build-global-artifacts
    # Only run if we're "publishing", and only if plan, local and global didn't fail (skipped is fine)
    if: ${{ always() && needs.plan.result == 'success' && needs.resolve-release-candidate.result == 'success' && needs.plan.outputs.publishing == 'true' && (needs.build-global-artifacts.result == 'skipped' || needs.build-global-artifacts.result == 'success') && (needs.build-local-artifacts.result == 'skipped' || needs.build-local-artifacts.result == 'success') }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      actions: read
      contents: read
    runs-on: "ubuntu-22.04"
    steps:
      # C-412: the first thing this job does is refuse to publish nothing. `if:` above admits
      # `skipped` builds because promoting a prepared candidate legitimately skips them — but with
      # no candidate to promote, `skipped` means this run compiled nothing, and every step below
      # would then create a Release out of an empty artifact directory. That is how v0.47.0 shipped:
      # the builds skipped, `host` ran anyway, and `gh release create` published a Release whose one
      # asset was `dist-manifest.json`.
      #
      # This is the cheap outer check — it reads job results, so it fails in seconds without a
      # checkout. The `Verify the artifact set before publishing` step below is the one that
      # inspects the actual bytes; this one exists so the common case fails early and legibly.
      - name: Refuse to publish a release with nothing built
        env:
          CANDIDATE_RUN_ID: ${{ needs.resolve-release-candidate.outputs.run-id }}
          PLAN_MANIFEST: ${{ needs.plan.outputs.val }}
          LOCAL_RESULT: ${{ needs.build-local-artifacts.result }}
          GLOBAL_RESULT: ${{ needs.build-global-artifacts.result }}
        run: |
          if [ -n "$CANDIDATE_RUN_ID" ]; then
            echo "Promoting candidate run $CANDIDATE_RUN_ID; its artifacts were verified complete by scripts/find-release-candidate.sh"
            exit 0
          fi
          if [ -z "$PLAN_MANIFEST" ]; then
            echo "::error::the plan job produced no build matrix for this run, so no build job could" \
              "be scheduled and there is nothing to release. If the job log shows \"Skip output" \
              "'val' since it may contain secret\", the manifest matched a masked repository secret" \
              "and Actions discarded it."
            exit 1
          fi
          failed=0
          for job in "build-local-artifacts:$LOCAL_RESULT" "build-global-artifacts:$GLOBAL_RESULT"; do
            if [ "${job#*:}" != "success" ]; then
              echo "::error::no prepared candidate exists and ${job%%:*} did not run to success" \
                "(${job#*:}); publishing now would create a Release with no binaries in it"
              failed=1
            fi
          done
          [ "$failed" -eq 0 ]
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install dist
        shell: bash
        run: scripts/install-release-tooling.sh
      # C-355 — the trust boundary of the whole build-once flow.
      #
      # This used to be `pattern: artifacts-*` with `merge-multiple: true`: the tag run took
      # whatever uploads happened to match at promotion time, and the merge decided what the
      # published directory contained. The receipt now names the seven archives by immutable ID,
      # size and digest; the consumer downloads each one by ID, hashes the RAW response bytes before
      # opening them, and extracts into a fresh per-record namespace with an extractor that refuses
      # traversal, absolute/drive/UNC names, control characters, special files, duplicate members
      # and cross-archive collisions. Only then is the host input assembled.
      - name: Download the candidate receipt
        if: ${{ needs.resolve-release-candidate.outputs.run-id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-candidate-receipt
          path: candidate
          run-id: ${{ needs.resolve-release-candidate.outputs.run-id }}
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
      - name: Verify and safely assemble the receipt-bound candidate bytes
        if: ${{ needs.resolve-release-candidate.outputs.run-id != '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          CANDIDATE_RUN_ID: ${{ needs.resolve-release-candidate.outputs.run-id }}
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          scripts/release-candidate.sh verify candidate/release-candidate.txt \
            "$version" "$GITHUB_SHA" "$CANDIDATE_RUN_ID"
          scripts/release-candidate.sh fetch candidate/release-candidate.txt \
            "$RUNNER_TEMP/candidate" --run-id "$CANDIDATE_RUN_ID"
          mkdir -p target/distrib artifacts
          cp -a "$RUNNER_TEMP/candidate/merged/." target/distrib/
          cp -a "$RUNNER_TEMP/candidate/merged/." artifacts/
      - name: Fetch compatibility-build artifacts
        if: ${{ needs.resolve-release-candidate.outputs.run-id == '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      # NOTE `--steps` is inert on the GitHub hosting backend in the pinned dist 0.32.0: its
      # `HostingStyle::Github` arm for `create` is empty ("implemented in CI backend"), and `upload`
      # and `release` are never read at all. Verified locally — `dist host --steps=create --tag=X`
      # and `dist plan --tag=X` emit byte-identical manifests apart from the build-id string, and
      # running it against the live repo left v0.47.1's Release untouched. What this step actually
      # produces is the manifest the announcement title and body below are read from. The Release is
      # created by the `gh release create` step further down; do not read these flags as the thing
      # that publishes.
      - id: host
        shell: bash
        run: |
          dist host ${{ needs.plan.outputs.tag-flag }} --allow-dirty --steps=upload --steps=release --output-format=json > dist-manifest.json
          echo "artifacts uploaded and released successfully"
          cat dist-manifest.json
          echo "manifest=$(jq -c "." dist-manifest.json)" >> "$GITHUB_OUTPUT"
          # The job output gets a prose-free projection, for the reason the plan job's does: the full
          # manifest embeds the release body, and Actions silently discards a job output that matches
          # a masked secret. This one was voided on the 0.47.0 run too. Nothing consumes it today —
          # the announcement steps below read the *step* output, which is not subject to that rule —
          # but leaving a second instance of the hazard in place only waits for a first consumer.
          echo "summary=$(jq -c '{announcement_tag, announcement_is_prerelease}' dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          # Overwrite the previous copy
          name: artifacts-dist-manifest
          path: dist-manifest.json
          retention-days: 14
      # The candidate path already populated `artifacts/` from the verified namespaces above; only
      # the compatibility rebuild still has anything to download here.
      - name: "Download compatibility-build GitHub artifacts"
        if: ${{ needs.resolve-release-candidate.outputs.run-id == '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Record promoted candidate
        if: ${{ needs.resolve-release-candidate.outputs.run-id != '' }}
        env:
          CANDIDATE_RUN_ID: ${{ needs.resolve-release-candidate.outputs.run-id }}
        run: |
          echo "Promoting immutable artifacts from candidate run $CANDIDATE_RUN_ID"
          echo "Source: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$CANDIDATE_RUN_ID" >> "$GITHUB_STEP_SUMMARY"
      - name: Cleanup
        run: |
          # Remove the granular manifests. NOTE: this glob also matches the plan job's
          # `plan-dist-manifest.json`, which the candidate-promotion path downloads — so the
          # top-level manifest has to be re-placed below or the release ships without it (C-47:
          # v0.27.0 was created with 27 assets but no dist-manifest.json and failed verification).
          rm -f artifacts/*-dist-manifest.json
          # The `dist host` step above wrote the release's own manifest to the workspace root; it
          # is a required release asset, so put it where `gh release create artifacts/*` sees it.
          cp dist-manifest.json artifacts/dist-manifest.json
      # C-412 — the fix, at the one place that decides whether a broken Release becomes public.
      #
      # `Create GitHub Release` below publishes `artifacts/*`, and `Verify GitHub Release` at the end
      # of this job checks what came out. That order is the defect: on v0.47.0's tag run (attempt 1,
      # host 12:54:47 → 12:55:08) the create step SUCCEEDED at 12:55:07 with an artifact directory
      # holding only `dist-manifest.json`, and the verify step then failed — against a Release that
      # was already public with `/releases/latest` pointing at it. A check after publication can only
      # report the damage.
      #
      # This is the same script, the same asset-set rules, run against the local directory before
      # anything is uploaded. It sits ahead of the attestation too, so an incomplete set is not
      # attested either.
      - name: Verify the artifact set before publishing
        run: scripts/verify-github-release.sh --staged artifacts
      # The checked set crosses to the authority-bearing jobs as bytes, not as a job output: the
      # announcement prose travels inside `artifacts/dist-manifest.json`, which is a release asset
      # anyway. A job output carrying that prose is silently dropped when it matches a masked
      # repository secret (C-412), and the two jobs below cannot afford to receive an empty title.
      #
      # The name deliberately does NOT start with `artifacts-`: that prefix is the candidate
      # receipt's closed producer set (C-355).
      - name: Hand the checked asset set to the publication jobs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-staged-assets
          path: artifacts/*
          if-no-files-found: error
          retention-days: 14

  # C-354: the attestation identity and nothing else. This job can mint a provenance statement; it
  # cannot create a Release, publish a crate, move a ref or read any long-lived secret.
  attest:
    needs:
      - plan
      - host
    if: ${{ needs.plan.outputs.publishing == 'true' }}
    runs-on: "ubuntu-22.04"
    environment: release
    permissions:
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Collect the checked asset set
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-staged-assets
          path: artifacts
      - name: Attest core release artifacts
        uses: actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d # v4
        with:
          subject-path: artifacts/*

  # C-354: the GitHub Release identity and nothing else. `RELEASE_TOKEN` appears once, in the `env`
  # of the create/upload step. It is not a workflow env, not a job env, and not readable by the
  # plan, build, hosting or attestation jobs — and it is never used to move a ref, create a tag,
  # dispatch a workflow or mint another credential.
  publish-github-release:
    needs:
      - plan
      - host
      - attest
    if: ${{ needs.plan.outputs.publishing == 'true' }}
    runs-on: "ubuntu-22.04"
    environment: release
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - name: Collect the checked asset set
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-staged-assets
          path: artifacts
      - name: Create GitHub Release
        env:
          # Scoped release token: a fine-grained PAT (this repo only, Contents: write) held by the
          # `release` environment, so the release object is created even when the org restricts the
          # default GITHUB_TOKEN or GitHub App integration from creating releases.
          # NOTE: release.yml is cargo-dist-generated — re-apply this override after `dist generate`.
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
          RELEASE_COMMIT: "${{ github.sha }}"
          RELEASE_TAG: "${{ needs.plan.outputs.tag }}"
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN}" ]; then
            echo "::error::RELEASE_TOKEN is not set on the release environment. Configure a fine-grained PAT for this repo with Contents: write; GITHUB_TOKEN has produced HTTP 403 on release creation."
            exit 1
          fi
          # Title and body come from the checked manifest that is already a release asset, so the
          # announcement prose never has to survive a job output.
          manifest=artifacts/dist-manifest.json
          title=$(jq -r '.announcement_title // empty' "$manifest")
          jq -r '.announcement_github_body // empty' "$manifest" > "$RUNNER_TEMP/notes.txt"
          [ -n "$title" ] || { echo "::error::the staged dist-manifest.json has no announcement title"; exit 1; }
          prerelease=""
          if [ "$(jq -r '.announcement_is_prerelease' "$manifest")" = "true" ]; then
            prerelease="--prerelease"
          fi

          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "Release $RELEASE_TAG already exists; refreshing assets with --clobber"
            gh release upload "$RELEASE_TAG" artifacts/* --clobber
          else
            for attempt in 1 2 3; do
              if gh release create "$RELEASE_TAG" --target "$RELEASE_COMMIT" $prerelease --title "$title" --notes-file "$RUNNER_TEMP/notes.txt" artifacts/*; then
                break
              fi
              if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
                echo "Release $RELEASE_TAG exists after create failure; refreshing assets with --clobber"
                gh release upload "$RELEASE_TAG" artifacts/* --clobber
                break
              fi
              if [ "$attempt" -eq 3 ]; then
                echo "::error::failed to create GitHub Release $RELEASE_TAG after $attempt attempts"
                exit 1
              fi
              sleep $((attempt * 15))
            done
          fi
      # Reading a public Release and its attestations needs no publication credential, so the
      # verifier runs on the job's own read-only GITHUB_TOKEN.
      - name: Verify GitHub Release
        env:
          GH_TOKEN: "${{ github.token }}"
        run: scripts/verify-github-release.sh "${{ needs.plan.outputs.tag }}"

  announce:
    needs:
      - plan
      - publish-github-release
    # use "always() && ..." to allow us to wait for all publish jobs while
    # still allowing individual publish jobs to skip themselves (for prereleases).
    # The GitHub Release job however must run to completion, no skipping allowed!
    if: ${{ always() && needs.publish-github-release.result == 'success' }}
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
          submodules: recursive
