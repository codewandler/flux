[package]
name = "sql"
edition.workspace = true
version.workspace = true
license.workspace = true

[[bin]]
name = "flux-plugin-sql"
path = "src/main.rs"

[dependencies]
host-kit.workspace = true
serde.workspace = true
serde_json.workspace = true
schemars.workspace = true
# D-31: the plugin no longer speaks the auth handshake (the host terminates it via
# `host.conn_authenticate`), so the SCRAM/MD5 crypto deps (hmac/sha2/rand) and base64 are gone —
# the plugin drives only the post-auth Simple Query protocol.
