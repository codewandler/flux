[package]
name = "flux-codegate"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "flux architecture lint: enforces that inner crates never depend on outer layers (the codegate analog)"

[dependencies]
proc-macro2.workspace = true
syn.workspace = true

[dev-dependencies]
cargo_metadata.workspace = true
