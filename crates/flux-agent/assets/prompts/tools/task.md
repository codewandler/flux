# Sub-agent operation

Use `task` only for a large, self-contained investigation or implementation that benefits from an
isolated role. Do not delegate ordinary reads or fragment one tightly coupled goal into many agents.
