---
id: E-22
title: "The generic `connector` channel kind — one arm instead of one adapter per vendor"
tracker: D-215
---

# The generic `connector` channel kind — one arm instead of one adapter per vendor

## Why

Its history was written down in [D-215](../stories/D-215-connector-channel-kind-epic.md), which stays the narrative record.

## Success criteria

- [ ] [NEEDS AUTHORING] A measurable outcome this epic delivers, distinct from the sum of its
      stories. Seeded by the C-742 migration; `flux board check` counts it as owed.

## Exit criteria

- [ ] Every story carrying `epic: connector-channels` is `done` (`flux board epics --slug connector-channels`).
- [ ] Every success criterion above is ticked.
