---
id: E-17
title: "Batched operation inputs — fewer calls for repeatable observations"
design: docs/designs/batched-operation-inputs.md
tracker: C-584
---

# Batched operation inputs — fewer calls for repeatable observations

## Why

The design behind this epic is [docs/designs/batched-operation-inputs.md](../designs/batched-operation-inputs.md).
Its history was written down in [C-584](../stories/C-584-batched-operation-inputs-epic.md), which stays the narrative record.

## Success criteria

- [ ] [NEEDS AUTHORING] A measurable outcome this epic delivers, distinct from the sum of its
      stories. Seeded by the C-742 migration; `flux board check` counts it as owed.

## Exit criteria

- [ ] Every story carrying `epic: batched-operation-inputs` is `done` (`flux board epics --slug batched-operation-inputs`).
- [ ] Every success criterion above is ticked.
