---
id: E-89
title: "Plugin OAuth (generalize the provider-only auth flow to plugins)"
design: docs/designs/plugin-oauth.md
---

# Plugin OAuth (generalize the provider-only auth flow to plugins)

## Why

The design behind this epic is [docs/designs/plugin-oauth.md](../designs/plugin-oauth.md).

## Success criteria

- [ ] [NEEDS AUTHORING] A measurable outcome this epic delivers, distinct from the sum of its
      stories. Seeded by the C-742 migration; `flux board check` counts it as owed.

## Exit criteria

- [ ] Every story carrying `epic: plugin-oauth` is `done` (`flux board epics --slug plugin-oauth`).
- [ ] Every success criterion above is ticked.
