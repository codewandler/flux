---
id: E-60
title: "flux-render — a built-in `flow_render` tool: flux source/plan → SVG (+ later PNG)"
design: docs/designs/flux-render.md
---

# flux-render — a built-in `flow_render` tool: flux source/plan → SVG (+ later PNG)

## Why

The design behind this epic is [docs/designs/flux-render.md](../designs/flux-render.md).

## Success criteria

- [ ] [NEEDS AUTHORING] A measurable outcome this epic delivers, distinct from the sum of its
      stories. Seeded by the C-742 migration; `flux board check` counts it as owed.

## Exit criteria

- [ ] Every story carrying `epic: flux-render` is `done` (`flux board epics --slug flux-render`).
- [ ] Every success criterion above is ticked.
